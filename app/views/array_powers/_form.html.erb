<%= form_for(@array_power) do |f| %>
  <% if @array_power.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@array_power.errors.count, "error") %> prohibited this array_power from being saved:</h2>

      <ul>
      <% @array_power.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
