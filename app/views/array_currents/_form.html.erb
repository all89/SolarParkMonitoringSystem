<%= form_for(@array_current) do |f| %>
  <% if @array_current.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@array_current.errors.count, "error") %> prohibited this array_current from being saved:</h2>

      <ul>
      <% @array_current.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
