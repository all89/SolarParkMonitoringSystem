<%= form_for(@array_voltage) do |f| %>
  <% if @array_voltage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@array_voltage.errors.count, "error") %> prohibited this array_voltage from being saved:</h2>

      <ul>
      <% @array_voltage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
