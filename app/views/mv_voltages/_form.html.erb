<%= form_for(@mv_voltage) do |f| %>
  <% if @mv_voltage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mv_voltage.errors.count, "error") %> prohibited this mv_voltage from being saved:</h2>

      <ul>
      <% @mv_voltage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
